#include "dps/core/publisher.h"

namespace dps {
    template<typename MessageT>
    Publisher<MessageT>::Publisher(std::string name){
        
    }

    template<typename MessageT>
    Publisher<MessageT>::~Publisher(){
        
    }
}
